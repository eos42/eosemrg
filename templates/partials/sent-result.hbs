<div class="sent-result">
  <h3>Producers alerted ({{session.flash.sentResult.length}}):</h3>
  <div class="card">
    <table class="table table-bordered table-sm sent-result-table">
      <tbody>
      {{#each session.flash.sentResult}}
        <tr>
          <td>
            {{this}}
          </td>
        </tr>
      {{/each}}
      </tbody>
    </table>
  </div>
</div>

<style>
  .sent-result {
    text-align: center;
    max-width: 350px;
    margin: 50px auto 0;
  }
  .sent-result-table {
    text-align: center;
    margin-bottom: 0;
    table-layout: fixed;
    width: 100%;
  }
</style>